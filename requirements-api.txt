# ============================================================
# API/worker deployment requirements — the serving image only.
#
# Everything here is imported at runtime by api/, agents/, db/,
# or services/worker. The full requirements.txt keeps the heavy
# extras this image deliberately omits:
#   pyannote.audio (torch, ~2GB — audio phase not built)
#   playwright + bs4 (HLTV crawler runs in GitHub Actions, not here)
#   celery (unused fallback; the DB job queue is primary)
#   langchain-google-vertexai / google-cloud-aiplatform (nothing imports them)
# Dropping them takes the image from multi-GB to a few hundred MB,
# which is what makes scale-to-zero cold starts tolerable.
# ============================================================

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- LangGraph / LangChain ---
langgraph==1.2.2
langchain==1.3.2
langchain-google-genai>=4.0.0
langchain-community>=0.3.31
langgraph-checkpoint-postgres>=2.0.0

# --- Google Cloud ---
google-cloud-storage>=2.18.2
google-cloud-tasks>=2.16.3
google-auth>=2.35.0

# --- Database ---
psycopg2-binary==2.9.9
sqlalchemy==2.0.35
pgvector==0.3.5
alembic>=1.13.0

# --- RCON ---
rcon==2.4.9

# --- Utilities ---
python-dotenv==1.0.1
pydantic>=2.9.2
pydantic-settings>=2.6.0
httpx>=0.28.1
requests>=2.32.5
tenacity>=9.0.0
google-genai>=1.65.0

# --- Observability ---
langsmith>=0.3.0

# --- Auth ---
python-jose[cryptography]>=3.3.0

# --- MCP ---
mcp>=1.0.0

# --- Vector DB ---
qdrant-client>=1.9.0

# --- Caching / OAuth state ---
redis>=5.0.0
