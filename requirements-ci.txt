# ============================================================
# CI requirements — pure Python only, no native C extensions
# Used by GitHub Actions: lint + type-check + unit tests only
#
# Deliberately minimal: only what the test suite actually imports.
# Full stack (langchain, GCP, pyannote, etc.) → requirements.txt
# ============================================================

# --- Web Framework ---
fastapi==0.115.0
python-multipart==0.0.9
httpx==0.27.2

# --- Database (pure Python ORM — SQLite fallback for CI) ---
sqlalchemy==2.0.35

# --- Utilities ---
python-dotenv==1.0.1
pydantic==2.9.2
tenacity==9.0.0
requests==2.32.3

# --- Test tooling ---
pytest
pytest-asyncio

# --- LangGraph & AI Dependencies ---
langgraph==1.2.2
google-genai>=1.65.0
langchain==1.3.2
langchain-google-genai>=4.0.0
langchain-community>=0.3.31

# --- RCON ---
rcon==2.4.9
