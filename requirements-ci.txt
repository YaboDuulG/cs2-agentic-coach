# ============================================================
# CI requirements — lightweight, no native binary deps
# Used by GitHub Actions: lint + type-check + unit tests only
# ============================================================

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- LangGraph / LangChain (pure Python wheels) ---
langgraph==0.2.50
langchain==0.3.7
langchain-google-vertexai==2.0.5
langchain-community==0.3.7

# --- Google Cloud (auth + clients) ---
google-cloud-storage==2.18.2
google-cloud-tasks==2.16.3
google-cloud-aiplatform==1.70.0
google-auth==2.35.0

# --- Database ---
psycopg2-binary==2.9.9
sqlalchemy==2.0.36
pgvector==0.3.5

# --- Utilities ---
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0
httpx==0.27.2
tenacity==9.0.0

# --- Observability ---
langsmith==0.1.143

# NOTE: The following are excluded from CI to avoid native binary build failures.
# Install manually in full dev / production environments:
#   pip install pyannote.audio==3.3.2
#   pip install langgraph-checkpoint-postgres==2.0.6
