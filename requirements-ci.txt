# ============================================================
# CI requirements — pure Python, no native C extensions
# Used by GitHub Actions: lint + type-check + unit tests only
#
# Deliberately minimal: only what the test suite actually imports.
# Full stack (langchain, GCP, pyannote, etc.) → requirements.txt
# ============================================================

# --- Web Framework (FastAPI test client needs these) ---
fastapi==0.115.0
python-multipart==0.0.9
httpx==0.27.2

# --- Utilities ---
python-dotenv==1.0.1
pydantic==2.9.2
tenacity==9.0.0

# --- Test tooling ---
pytest
pytest-asyncio
